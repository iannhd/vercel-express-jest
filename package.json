{
  "name": "sandbox",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "jest --forceExit",
    "builds": [
      { "src": "app.js", "use": "@vercel/node" }
   ],
   "routes": [
      { "src": "/(.*)", "dest": "/app.js" }
   ]
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "firebase": "^9.8.4",
    "jest": "^28.1.2",
    "morgan": "~1.9.1",
    "supertest": "^6.2.4"
  }
}
